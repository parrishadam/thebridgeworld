# ──────────────────────────────────────────────────────────────────────────────
# The Bridge World — environment variables
#
# Copy this file to .env.local and fill in your values.
# Never commit .env.local to version control.
# ──────────────────────────────────────────────────────────────────────────────

# ── Clerk (authentication) ─────────────────────────────────────────────────
# Find these in https://dashboard.clerk.com → your app → API Keys

# Publishable key (public — safe to expose)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_your_key_here

# Secret key (server-side only, keep secret)
CLERK_SECRET_KEY=sk_test_your_key_here

# Route configuration
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/

# ── Supabase ───────────────────────────────────────────────────────────────
# Find these in https://supabase.com/dashboard → your project → Settings → API

# Project URL (public — safe to expose)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Anon/public key (public — safe to expose, respects RLS)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here

# Service role key (secret — server-side only, bypasses RLS)
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# ── Sanity (Next.js / front-end) ───────────────────────────────────────────
# Find these in https://www.sanity.io/manage → your project → API

# Project ID (public — safe to expose)
NEXT_PUBLIC_SANITY_PROJECT_ID=your_project_id_here

# Dataset name (usually "production")
NEXT_PUBLIC_SANITY_DATASET=production

# Read token — used only for draft/preview mode (server-side only, keep secret)
SANITY_API_READ_TOKEN=your_read_token_here

# ── Sanity Studio (sanity/ directory) ──────────────────────────────────────
# These are used when running `npm run dev` inside /sanity

SANITY_STUDIO_PROJECT_ID=your_project_id_here
SANITY_STUDIO_DATASET=production
